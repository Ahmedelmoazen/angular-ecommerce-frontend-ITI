import { StringControlPipe } from './string-control.pipe';

describe('StringControlPipe', () => {
  it('create an instance', () => {
    const pipe = new StringControlPipe();
    expect(pipe).toBeTruthy();
  });
});
